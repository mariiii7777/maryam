<form method="post" action="{{ path('app_basic_bundle_delete', {'id': basic_bundle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basic_bundle.id) }}">
    <button class="btn">Delete</button>
</form>
